// import { API } from '../../configs/apis';

export const EF24Login = async ({ username, password }) => {
  try {
    // console.log('user', username, password);
    // const body = {
    //   username,
    //   password,
    // };
    // const result = await API.post('/login', body);
    // return null;
  } catch (error) {
    // return {
    //   success: false,
    //   msg: error,
    // };
  }
};
